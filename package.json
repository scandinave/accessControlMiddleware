{
  "name": "accesscontrolmiddleware",
  "version": "1.0.0",
  "description": "Express Middleware for AccessControl library that support generics, specifics and dynamics permissions check",
  "main": "app.js",
  "scripts": {
    "test": "export NODE_ENV=test && nyc --reporter=html --reporter=lcov mocha --timeout 10000 --exit"
  },
  "keywords": [
    "HRBAC"
  ],
  "author": "LE BARO Romain",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "chai-like": "^1.1.1",
    "chai-match": "^1.1.1",
    "chai-sorted": "^0.2.0",
    "chai-things": "^0.2.0",
    "eslint": "^5.3.0",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2"
  },
  "dependencies": {
    "jsonwebtoken": "^8.3.0"
  }
}
